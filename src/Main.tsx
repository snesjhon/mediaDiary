import * as React from "react";
import { Box, Button } from "./components";

const Main = () => {
  return (
    <div>
      <Button variant="primary">something</Button>
    </div>
  );
};

export default Main;
