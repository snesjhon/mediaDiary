export interface MediaTypes {
  type: "film" | "tv" | "album" | "";
}
export interface Style {
  colors: {
    primary: string;
    secondary: string;
    orange: string;
    blue: string;
    "bg-primary": string;
    "bg-secondary": string;
    "border-primary": string;
    "border-secondary": string;
  };
}
